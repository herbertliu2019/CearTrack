# Memory module uses integration.py for registration (cpu/wipe-style).
# This stub satisfies the module_registry auto-discovery scan without
# exposing a `blueprint` attribute, so core/module_registry.py skips it
# and integration.py's explicit registration in app.py is authoritative.
